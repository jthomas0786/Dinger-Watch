{
  "name": "pulse-sports",
  "version": "1.0.0",
  "private": true,
  "description": "Live sports scoreboard and fan-pulse web app.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
